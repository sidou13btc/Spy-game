name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          git wget unzip \
          openjdk-17-jdk \
          python3-pip \
          autoconf libtool pkg-config \
          zlib1g-dev libncurses5-dev \
          cmake libffi-dev libssl-dev

    - name: Install Cython and buildozer
      run: |
        pip install --upgrade pip
        pip install cython buildozer

    - name: Cache Android SDK/NDK
      uses: actions/cache@v4
      with:
        path: ~/.buildozer
        key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}

    - name: Accept Android licenses automatically
      run: |
        # هذا سيقبل التراخيص المطلوبة لـ sdkmanager
        yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses || true
        # أو يمكنك استخدام الأمر التالي إذا كان متغير ANDROID_HOME غير مضبوط
        # yes | /usr/local/lib/android/sdk/tools/bin/sdkmanager --licenses

    - name: Build with buildozer
      run: |
        # استخدام buildozer مباشرة بدون pipe، سيطلب التراخيص لكننا قبلناها بالفعل
        buildozer android debug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: bin/*.apk
